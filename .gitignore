# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs / build artifacts at repo root
*.log

# Backend (Laravel) — most ignores live in backend/.gitignore
backend/.env
backend/.env.backup
backend/.env.production

# Frontend (Vite) — most ignores live in frontend/.gitignore
frontend/.env
frontend/.env.local
frontend/.env.*.local

# Dependencies and build outputs (covered by sub-gitignores, listed here for safety)
node_modules/
vendor/
dist/
build/
